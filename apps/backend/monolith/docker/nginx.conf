server {
    listen 80;

    # Health check endpoint for K8s probes
    location /healthz {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Static files for Django
    location /static/ {
        alias /staticfiles/;
    }
}
