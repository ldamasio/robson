// Port: PatternService
// Pattern Detection Engine API service interface.
// All methods return Promises and do not touch UI concerns.

export class PatternService {
  // Get pattern catalog (all available patterns)
  // async getCatalog() {}

  // Get pattern dashboard summary
  // async getDashboard() {}

  // Get recent confirmed alerts
  // async getRecentConfirms({ hours, symbol, patternCode }) {}

  // List pattern instances with filters
  // async getInstances({ symbol, timeframe, status, patternCode, limit }) {}

  // List pattern alerts with filters
  // async getAlerts({ symbol, timeframe, alertType, hours, limit }) {}

  // Get strategy pattern configurations
  // async getConfigs() {}

  // Create strategy pattern configuration
  // async createConfig(configData) {}

  // Update strategy pattern configuration
  // async updateConfig(configId, configData) {}

  // Delete strategy pattern configuration
  // async deleteConfig(configId) {}

  // Trigger pattern scan
  // async triggerScan({ symbols, timeframes, allDetectors }) {}

  // Process confirmed pattern to plan
  // async patternToPlan({ patternInstanceId, forceCreate }) {}
}
