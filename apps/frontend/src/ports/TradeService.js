// Port: TradeService
// Methods should return Promises and not touch UI concerns.

export class TradeService {
  // async placeOrder({ symbol, side, qty, limit }) {}
  // async getStrategies({ authToken }) {}
}

