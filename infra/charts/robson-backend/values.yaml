image:
  repository: ldamasio/rbs-backend-monolith-prod
  tag: latest
  pullPolicy: IfNotPresent

replicaCount: 1

service:
  port: 8000

host: backend.robson.rbx.ia.br

gateway:
  className: istio

resources: {}

