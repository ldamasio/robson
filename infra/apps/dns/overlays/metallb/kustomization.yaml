apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dns

resources:
  - ../../base/namespace.yaml
  - ../../base/postgresql.yaml
  - ../../base/powerdns-config.yaml
  - ../../base/pdb.yaml
  - powerdns-deployment.yaml
  - services.yaml
  - metallb-config.yaml

# Secrets should be managed via SealedSecrets or SOPS in production
# For initial deployment, update base secrets manually

commonLabels:
  deployment-scenario: metallb
  app.kubernetes.io/part-of: dns-infrastructure
