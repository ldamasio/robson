# VPS Root Passwords - NEVER COMMIT THIS FILE

# This file contains sensitive credentials
# Usage: ansible-playbook ... --extra-vars "@inventory/contabo/passwords.yml"

---
# Single password for all 4 VPS (simplified)
ansible_ssh_pass: "CHANGE_ME_YOUR_VPS_PASSWORD"
