# /root/apps/prz/frontend/nginx.config

server {
    listen       ${PORT:-3403};
    server_name  robson.srv.br;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $$uri /index.html;
    }
}
