//! Robson v2 Storage Layer
//!
//! PostgreSQL persistence with event sourcing and leader election.

#![warn(clippy::all)]

// Module declarations will be added as we implement

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
